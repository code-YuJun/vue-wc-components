%%{init: {'theme': 'base'}}%%
graph TD

%% ================== 1. 开发构建层 ==================
subgraph DevBuild [1. 开发 & 构建层]
    A1["组件源码<br/>(Vue / WebComponent)"]
    A2["构建工具<br/>Vite / Rollup"]
    A3["构建产物<br/>dist/extension.min.js"]
    A4["构建产物<br/>dist/style.css"]

    A1 --> A2
    A2 --> A3
    A2 --> A4
end

%% ================== 2. 分发层 ==================
subgraph Distribution [2. 分发层]
    B1["npm registry"]
    B2["unpkg CDN"]

    A3 --> B1
    A4 --> B1
    B1 --> B2
end

%% ================== 3. 配置中心 ==================
subgraph ConfigCenter [3. 配置系统]
    C1["组件配置 API"]
    C2["组件元数据<br/>{name, version, enabled}"]

    C1 --> C2
end

%% ================== 4. 运行时（核心） ==================
subgraph Runtime [4. Loader Runtime（核心）]

    R1["loader.js 入口"]
    R2["解析 data-components"]
    R3["fetchMetadata()"]

    R4["组件过滤<br/>(用户需求 ∩ 配置系统)"]

    subgraph ResourceLoad [资源加载]
        RL1["加载 CSS<br/><link>"]
        RL2["加载 JS<br/><script>"]
    end

    subgraph Register [注册流程]
        RG1["window.Extension.register()"]
        RG2["customElements.define"]
    end

    subgraph Event [事件机制]
        EV1["dispatch: extension-ready"]
    end

    R1 --> R2
    R2 --> R3
    R3 --> R4

    R4 --> RL1
    R4 --> RL2

    RL2 --> RG1
    RG1 --> RG2

    RG2 --> EV1
end

%% ================== 5. 业务消费层 ==================
subgraph Consumer [5. 业务页面]
    D1["HTML 使用<br/><my-card />"]
    D2["JS 监听事件<br/>extension-ready"]
end

%% ================== 连接 ==================

%% CDN → Runtime
B2 --> RL1
B2 --> RL2

%% 配置 → Runtime
C2 --> R4

%% Runtime → 页面
RG2 --> D1
EV1 --> D2